package com.disastermgmt;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic test to verify that the Spring Boot application context
 * loads successfully — i.e., all beans are wired correctly and
 * the app can start.
 */
@SpringBootTest
class DmsApplicationTests {

    @Test
    void contextLoads() {
        // This test passes if the application context starts without errors.
        // If it fails, it means there's a configuration or dependency problem.
    }
}
