# ============================================================
# FastAPI AI Service — Requirements
# ============================================================
# These are the Python packages this service needs.
# Install with: pip install -r requirements.txt
#
# For PyTorch CPU-only (saves ~1.5 GB disk space):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# ============================================================

# --- Web Framework ---
fastapi>=0.111.0          # The API framework (like Spring Boot but for Python)
uvicorn[standard]>=0.30.0 # The web server that runs FastAPI
python-multipart>=0.0.9   # Needed for file upload support

# --- Data / Math ---
numpy>=1.26.0             # Numerical computing (arrays, math operations)
pillow>=10.3.0            # Image loading and basic manipulation
opencv-python-headless>=4.9.0  # Advanced image processing (tiling, resizing)

# --- AI / ML (already installed on your machine) ---
# torch>=2.3.0            # Deep learning framework (already installed)
# torchvision>=0.18.0     # Image transforms for PyTorch (already installed)

# --- Geometry / GeoJSON ---
shapely>=2.0.0            # Geometric operations (polygons, intersections)
geojson>=3.1.0            # Create and validate GeoJSON objects

# --- HTTP Client ---
httpx>=0.27.0             # Modern HTTP client (for calling other services)

# --- Routing (for later milestones) ---
# osmnx>=2.0.0            # Road network from OpenStreetMap
# networkx>=3.3            # Graph algorithms (Dijkstra, A*)

# --- Evaluation (for later milestones) ---
# scikit-learn>=1.5.0      # Metrics (F1, precision, recall, confusion matrix)
